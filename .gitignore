# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# WordPress uploads (data files stored by the plugin at runtime)
# These should not be committed to the repo.
uploads/

# Composer (not used, but just in case)
vendor/
composer.lock

# Node (not used, but just in case)
node_modules/
package-lock.json
